const size = {
  xs: "320px",
  sm: "768px",
  lg: "1200px",
};

const theme = {
  colors: {
    primary: "#0070f3",
  },
  xs: `(min-width: ${size.xs})`,
  sm: `(min-width: ${size.sm})`,
  lg: `(min-width: ${size.lg})`,
};

export default theme;
